id: "purple"
apiVersion: "1"
version: "1.0"
description: "A simple bundle that sets up a master with the pipeline template catalog"
parent: "community2"
plugins:
  - "plugins.yaml"
items:
  - "items/"

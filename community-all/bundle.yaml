id: "community-all"
apiVersion: "1"
version: "2"
description: "A bundle using all the community (tier 3) plugins that are allowed"
parent: "community-base"
availabilityPattern: ".*"
plugins:
  - "plugins.yaml"

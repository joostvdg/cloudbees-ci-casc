id: "community-all"
version: "1"
apiVersion: "1"
description: "A bundle using all the community (tier 3) plugins that are allowed"
parent: "community-base"
plugins:
  - "plugins.yaml"
jcasc:
  - "jenkins.yaml"
catalog:
  - "plugin-catalog.yaml"
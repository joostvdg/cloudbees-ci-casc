id: "git-bundle-community-all"
apiVersion: "1"
version: "3"
description: "A bundle using all the community (tier 3) plugins that are allowed"
parent: "communitybase"
availabilityPattern: ".*"
jcasc:
  - "jenkins.yaml"
plugins:
  - "plugins.yaml"

id: "community_all"
apiVersion: "1"
version: "3"
description: "A bundle using all the community (tier 3) plugins that are allowed"
parent: "community_base"
availabilityPattern: ".*"
plugins:
  - "plugins.yaml"

id: "community-base"
version: "1"
apiVersion: "1"
description: "A base bundle including allowed community (tier 3) plugins via the Plugin Catalog."
plugins:
  - "plugins.yaml"
jcasc:
  - "system/"
catalog:
  - "plugin-catalog.yaml"
